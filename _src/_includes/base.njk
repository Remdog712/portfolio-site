<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  <!-- Add pathPrefix to CSS -->
  <link rel="stylesheet" href="{{ pathPrefix }}css/styles.css" />
  
  <!-- Add pathPrefix to favicon -->
  <link rel="icon" href="{{ pathPrefix }}RO_Web.ico" type="image/x-icon" />
  
  <!-- External font link (no change needed for external URLs) -->
  <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
  <header class="header">
    <div class="header-content">
      <div class="logo">
        <!-- Add pathPrefix to image sources -->
        <a href="{{ pathPrefix }}">
          <img src="{{ pathPrefix }}images/RO_Logo.PNG" alt="RO Logo" class="logo-image" />
        </a>
        <a href="{{ pathPrefix }}">
          <img src="{{ pathPrefix }}images/NameLogo.png" alt="Name Logo" class="name-logo" />
        </a>
      </div>
      <!-- Navbar inclusion (no changes needed here if it uses relative paths internally) -->
      {% include "layouts/navbar.html" %}
    </div>
  </header>
  <div class="main-content">
    {{ content | safe }}
  </div>
</body>
</html>
